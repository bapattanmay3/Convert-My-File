services:
  - type: web
    name: convert-my-file
    runtime: python
    plan: free
    buildCommand: "./render-build.sh"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
